var n = prompt("What number would you like to calculate to the power of 3?");
var cube = function (n) {
  if (n === number) {
    return n * n * n;
  } else {
    alert("That is not a number");
  }
};

alert(cube(n));